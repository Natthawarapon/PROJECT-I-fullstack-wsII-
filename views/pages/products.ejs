<% include ../partials/header %>

<div class="container">
 <h3 >Products Information</h3> 
    <input class="form-control" id="myInput" type="text" placeholder="Search..">
    <br>
<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Price</th>
               <th>Edit</th>
            <th>Delete</th>
            <th><a name="" id="" class="btn btn-primary" href="/insert" role="button">Add New</a></th>
        </tr>
    </thead>
    <tbody id="myTable" >
       <% products.forEach(function(product){ %> 
       <tr>
            <td scope="row"><%= product.id%></td>
            <td><%= product.title%></td>
            <td><%= product.price%></td>
             <td><a name="" id="" class="btn btn-danger" href="/products/<%= product.id%>" role="button">Edit</a></td>
              <td><a name="" id="" class="btn btn-danger" href="/productsdelete/<%= product.id%>" role="button">Delete</a></td>
        
        </tr>
         <% }); %>
</tbody>

    
</table>
<script>
    $(document).ready(function(){
      $("#myInput").on("keyup", function() {
        var value = $(this).val().toLowerCase();
        $("#myTable tr").filter(function() {
          $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
        });
      });
    });
    </script>
    
</div>

 <% include ../partials/footer %>